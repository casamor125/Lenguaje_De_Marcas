<%- include ("header") %>

    <form action="/AfegirComanda" method="post" class="container-sm mt-5">
        <!-- <div class="mb-3">
          <label for="nom" class="form-label">nom</label>
          <input type="text" class="form-control" name="nom" id="nom" required>
          <div  class="form-text">Insert the name of your product</div>
        </div>
        <div class="mb-3">
            <label for="preu" class="form-label">preu</label>
            <input type="number" class="form-control" name="preu" id="preu" min="0" required>
            <div class="form-text">Insert the price for the new product</div>
          </div>
        <button type="submit" class="btn btn-primary ">Submit</button> -->

        <label for="usuaris">Eligeix un usuari: </label>

          <select name="usuari" id="usuari">
            <% msgs1.forEach((msg, usuaris)=>{ %>

              <option value="<%= msg.id %>"><%= msg.nom %></option>
            
            <% })   %>
          </select>

          <label for="productes">Eligeix un producte:</label>

          <select name="producte" id="producte">
            <% msgs2.forEach((msg, productes)=>{ %>

              <option value="<%= msg.id %>"><%= msg.nom %></option>
            
            <% })   %>
          </select>
          <button type="submit" class="btn btn-primary ">Submit</button>
      </form>
<%- include("footer")%>